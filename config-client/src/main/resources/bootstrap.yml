spring:
  application:
    name: eureka-fegin
  #    name: config-client
  cloud:
    config:
      discovery:
        service-id: config-server-git
        enabled: true
      profile: dev
      label: master

management:
  security:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka